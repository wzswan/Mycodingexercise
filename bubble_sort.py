# -*- coding: utf-8 -*-
"""
Created on Tue Nov 29 14:27:28 2016

@author: wzswan
"""

def bubble=sort(lists)：:
    cout = len(lists)
    for i in range(0, count):
        for j in range(i + 1, count):
            if lists[i] > lists[j]:
                lists[i], lists[j] = lists[j], lists[i]
    return lists